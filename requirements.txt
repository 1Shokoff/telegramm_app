aiogram>=3.13,<4.0
aiohttp>=3.10,<4.0
aiosqlite>=0.20
# Нужен, только когда задан TELEGRAM_PROXY: aiogram гоняет запросы к Telegram
# через прокси именно этой библиотекой (socks5 и http).
aiohttp-socks>=0.9
